-- chat:
["ChatEnabled"] = true, -- used to enable disable chat
["BubbleChatEnabled"] = true, -- used to enable disable bubble chat, depended on ChatEnabled, but can be disabled if chat is enabled
["ChatAnchoredPosition"] = 1, -- where the chat is anchored.
["ChatHorizontalSize"] = .3, -- how big the chat is, horizontally

-- chat messages:
["ChatMessageDisplayCount"] = 15, -- how many messages the chat displays
["ChatMessageDestroyDelay"] = 20, -- how many seconds until a message gets destroyed, in seconds

[""] = nil,
